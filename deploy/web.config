<?xml version="1.0" encoding="utf-8"?>
<!--
  IIS (Windows) → Next.js standalone vía HttpPlatformHandler.
  Coloca este web.config en la RAÍZ de la app (junto a server.js).

  Requisitos en el servidor IIS:
    - Node.js instalado (ajusta processPath si no es la ruta por defecto).
    - Módulo "HttpPlatformHandler" instalado en IIS.
      (En Azure App Service Windows ya está disponible.)

  Variables de entorno: puedes definirlas aquí (abajo) o, más fácil, dejar un
  archivo .env.local junto a server.js (Next lo carga al arrancar). En Azure App
  Service puedes usar las "Application settings".
-->
<configuration>
  <system.webServer>
    <handlers>
      <add name="httpPlatformHandler" path="*" verb="*" modules="httpPlatformHandler" resourceType="Unspecified" requireAccess="Script" />
    </handlers>

    <httpPlatform stdoutLogEnabled="true"
                  stdoutLogFile=".\logs\node.log"
                  startupTimeLimit="60"
                  processPath="C:\Program Files\nodejs\node.exe"
                  arguments=".\server.js">
      <environmentVariables>
        <environmentVariable name="PORT" value="%HTTP_PLATFORM_PORT%" />
        <environmentVariable name="NODE_ENV" value="production" />
        <environmentVariable name="HOSTNAME" value="127.0.0.1" />

        <!-- Config de la app: descomenta y pon valores reales, o usa .env.local /
             App settings en lugar de esto.
        <environmentVariable name="GRAPH_TENANT_ID" value="" />
        <environmentVariable name="GRAPH_CLIENT_ID" value="" />
        <environmentVariable name="GRAPH_CLIENT_SECRET" value="" />
        <environmentVariable name="GRAPH_MAILBOX" value="demo-gpfleet@kubus-tech.com.mx" />
        <environmentVariable name="USERSERVICE_URL" value="https://gpfleetdemousersservice.azurewebsites.net/api/" />
        <environmentVariable name="JWT_ISSUER" value="Origami.Identity" />
        <environmentVariable name="JWT_AUDIENCE" value="Origami.Identity" />
        <environmentVariable name="JWT_SIGNING_KEY" value="" />
        -->
      </environmentVariables>
    </httpPlatform>
  </system.webServer>
</configuration>
